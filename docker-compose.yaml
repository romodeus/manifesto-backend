version: "3"

services:
  apps: 
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: shortly
    image: shortly
    ports: 
      - ${SERVER_PORT}:${SERVER_PORT}
    environment:
      SERVER_PORT: ${SERVER_PORT}
      BASE_URL: ${BASE_URL}
